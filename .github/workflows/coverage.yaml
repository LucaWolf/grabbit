name: Coverage
on:
  push:
    branches: [ master ]
  workflow_dispatch:
jobs:
  unix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: oldstable
          cache: true
      - name: Coverage report
        uses: LucaWolf/go-coverage-report@feat/go-build-tags
        with:
          amend: true
          build-tags: test_env
        continue-on-error: true
